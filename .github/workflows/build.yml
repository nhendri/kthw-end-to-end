name: Test Workflow

on:
    push:
        branches:
            - main

jobs:
    run-script:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout Code
              uses: actions/checkout@v2
            - name: Run Script
              run: |
                chmod +x hi.sh
                ./hi.sh
